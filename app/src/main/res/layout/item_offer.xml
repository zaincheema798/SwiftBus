<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_offer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/image_offer_icon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginEnd="12dp"
                android:scaleType="centerCrop"
                tools:src="@drawable/ic_offer"
                android:tint="#FF6B35" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/text_offer_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#1A1A1A"
                    tools:text="Weekend Special" />

                <TextView
                    android:id="@+id/text_offer_code"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="12sp"
                    android:textColor="#FF6B35"
                    android:textStyle="bold"
                    android:background="@drawable/bg_offer_code"
                    android:padding="4dp"
                    android:layout_marginTop="4dp"
                    tools:text="Code: WEEKEND25" />

            </LinearLayout>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_discount_badge"
                android:padding="8dp"
                android:layout_marginStart="8dp">

                <TextView
                    android:id="@+id/text_discount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/white"
                    android:gravity="center"
                    tools:text="25% OFF" />

            </LinearLayout>

        </LinearLayout>

        <!-- Description -->
        <TextView
            android:id="@+id/text_offer_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#555555"
            android:layout_marginTop="12dp"
            android:lineSpacingExtra="2dp"
            tools:text="Get 25% off on weekend bookings for Lahore to Karachi route" />

        <!-- Validity -->
        <TextView
            android:id="@+id/text_validity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#777777"
            android:layout_marginTop="8dp"
            android:drawableStart="@drawable/ic_clock_small"
            android:drawablePadding="4dp"
            android:gravity="center_vertical"
            tools:text="Valid until: Dec 31, 2024" />

        <!-- Terms and Conditions -->
        <TextView
            android:id="@+id/text_terms"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="#888888"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_terms"
            android:padding="8dp"
            tools:text="Minimum booking: PKR 2000 • Weekend bookings only" />

        <!-- Action Button -->
        <Button
            android:id="@+id/button_claim_offer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:padding="12dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/bg_claim_button"
        android:text="Claim Offer"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        android:ellipsize="none"
        android:singleLine="false"
        android:gravity="center"
        android:elevation="2dp"
        android:stateListAnimator="@null" />

    </LinearLayout>

</androidx.cardview.widget.CardView>